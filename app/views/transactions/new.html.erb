<h1 class ="title">New Transaction</h1>
<p id="notice"><%= notice %></p>
<%= render 'form', transaction: @transaction, owned_stocks: @owned_stocks %>

<div class="back-button">
    <%= button_to 'Back to Home Page', root_path, :method => "get", class: "btn btn-secondary btn-lg" %>
</div>