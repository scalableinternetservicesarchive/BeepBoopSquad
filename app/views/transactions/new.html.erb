<%= stylesheet_link_tag 'transactions'%>
<%= render 'shared/background', header: (render 'header'), 
body: (render 'form', transaction: @transaction, owned_stocks: @owned_stocks),
footer: (render 'footer') %>